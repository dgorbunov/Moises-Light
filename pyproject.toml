[project]
name = "mamba-light"
version = "0.1.0"
description = "Reproduction baseline for Moises-Light on MUSDB18"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = [
  "audio_io",
  "augment",
  "config",
  "data",
  "dataset_utils",
  "infer",
  "losses",
  "metrics",
  "model",
  "module",
  "overlap_add",
  "stft",
]
